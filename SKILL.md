---
name: quark-lazy-cli
description: Minimal ClawHub import test for the quark-lazy-cli skill.
---

# quark-lazy-cli

This is a minimal Skill import test.

Use this repository with qslazy to manage QAS and Quark subscription updates.

Important:

- Ask the user before running update commands.
- Do not expose QAS tokens.
- Use `LAZY_CLI_ADVISOR=code` for rule-based selection.
- Use `--add-prompt "kword=..."` to filter selected share-link titles in code mode.
